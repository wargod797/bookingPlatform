<<<<<<< HEAD
# bookingPlatform
=======
# Booking Platform Spring Boot Application

## Overview
This is a Spring Boot application with PostgreSQL (port 5432) and caching (Ehcache or Redis). Spring Boot 4.0.0 and Java 21 are used by default.

## Prerequisites
- Java 21 (or 25) SDK installed
- Maven (or use Maven Wrapper)
- Docker & Docker Compose (for container setup)

## Build and Run (Maven)
1. Set environment vars if needed (defaults: DB_USER=postgres, DB_PASS=postgres).
2. Build the JAR:
>>>>>>> d274526 (Complete Solution)
